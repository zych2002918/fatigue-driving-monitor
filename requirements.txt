# 核心运行依赖（PC 复现）
numpy>=1.24
opencv-python>=4.6
# Windows 上 dlib 无 cp311 官方 wheel，用 dlib-bin 预编译；Linux/mac 可直接 dlib
dlib-bin>=19.22; sys_platform == "win32"
dlib>=19.22; sys_platform != "win32"
wxPython>=4.2

# 可选：语音播报（WinsoundAlarmer use_tts=True）
# pyttsx3>=2.90

# 可选：重新生成 README 架构图（scripts/make_arch_diagram.py）
# matplotlib>=3.5
